export type AgentSnapshotKind = "cluster" | "gpu" | "traces" | "health";export type StateSource = "hub_agent" | "local_cache"; export interface AgentSnapshot<T = any> {   schemaVersion: number;   generatedAt: string;   expiresAt?: string;   ttlSeconds?: number;   hubId?: string;   workers?: any[];   errors?: string[];   partialFailure?: boolean;   source?: StateSource;   data?: T;   schedulerStates?: any[];   experimentTraces?: any[];   gpu?: Record<string, any[]>;   health?: Record<string, any>; }  export interface SnapshotFreshness {   fresh: boolean;   ageMs: number;   reason?: string; }  export function parseAgentSnapshot(raw: string): AgentSnapshot | undefined {   try {     const parsed = JSON.parse(raw);     if (!parsed || Number(parsed.schemaVersion) !== 1 || !parsed.generatedAt) return undefined;     return { ...parsed, source: "hub_agent" };   } catch {     return undefined;   } }  export function agentSnapshotFreshness(snapshot: AgentSnapshot | undefined, defaultTtlSeconds: number): SnapshotFreshness {   if (!snapshot) return { fresh: false, ageMs: Number.POSITIVE_INFINITY, reason: "missing" };   const generated = Date.parse(String(snapshot.generatedAt || ""));   if (!Number.isFinite(generated)) return { fresh: false, ageMs: Number.POSITIVE_INFINITY, reason: "bad_generatedAt" };   const ageMs = Date.now() - generated;   const expires = Date.parse(String(snapshot.expiresAt || ""));   if (Number.isFinite(expires) && Date.now() > expires) return { fresh: false, ageMs, reason: "expired" };   const ttlMs = Math.max(1, Number(snapshot.ttlSeconds || defaultTtlSeconds || 10)) * 1000;   if (ageMs > ttlMs) return { fresh: false, ageMs, reason: "ttl_expired" };   return { fresh: true, ageMs }; }  export function snapshotPath(kind: AgentSnapshotKind): string {   const file = kind === "cluster"     ? "cluster_snapshot.json"     : kind === "gpu"       ? "gpu_snapshot.json"       : kind === "traces"         ? "experiment_traces_snapshot.json"         : "health_snapshot.json";   return `zlk_cluster/tmp/cluster_agent/${file}`; } 
