# 目标模式当前计划：前后端静态优化周期
本文档只保留最新活动目标。历史批次、验证和部署记录以 git 提交为准。
打包/清理时会自动压缩本文件，禁止堆积流水账。

## 固定边界
- SimpleExperiment 负责计划、Agent、状态和任务；SimpleSFTP 负责真实文件传输；PPT 插件负责绘图。
- 连接边界固定为 Xshell 本地隧道 + Hub/Worker Agent + SimpleSFTP；插件不内置 SSH/SCP/rsync。
- 不迁移、删除或重写旧任务、结果和安装产物；不处理现有用户删除项、历史 VSIX 或 `zlk_cluster/ui/`。
- 当前不连接服务器，只执行本地静态检查；不重载、关闭或自动操作 VS Code。
- 每批最多 2 至 3 个强相关问题、8 个源码/文档/测试文件，并独立提交、推送到 `origin/master`。
- 每 5 个完成批次执行一次全量静态测试，其余批次执行定向检查。

## 后续优先级
- [已完成] 1/5 前端：缓存 GPU 历史点索引并优化 hover 邻近查询。
- [已完成] 2/5 GPU 历史：三天滑动窗口补零、持久化序列上限与 Webview 缓存上限。
- [已完成] 3/5 前端：避免维护阶段重复扫描全部原生 title 节点。
- [已完成] 4/5 后端：限制 Worker 操作节流时间戳缓存并清理过期键。
- [待做] 5/5 前后端：执行第十一轮非服务器静态测试。

## 当前批次：autonomous-static-054（已完成）
### 修复点

- 增加可复用的有界时间戳 Map 更新函数，按 TTL 清理已失效的非活跃键。
- Worker 操作节流时间戳最多保留 256 个非活跃 Worker，并按最近使用顺序淘汰。
- 保护当前执行中或排队准入的 Worker，避免清理破坏最小操作间隔和并发限制。

### 回归风险

- 活跃 Worker 的节流状态不得因容量裁剪被删除。
- 过期键清理不得改变当前 Worker 的最新时间戳或 Map 的 LRU 顺序。
- 不修改服务器请求频率配置、操作并发语义或 Xshell 通信边界。

### 验证清单

- [已通过] TypeScript、Lint 与 Extension 编译产物。
- [已通过] 有界时间戳 Map、操作生命周期和调度 UI 定向静态测试 19/19。

## 本批记录
- 上一批完成基线：`5e32215`，原生 title 维护扫描缓存已同步远程。
- 当前批次影响区：Worker 操作节流时间戳缓存、通用有界 Map 工具与对应测试。
- 只清理已过期或超限的非活跃键；当前执行或准入排队键受保护，不触发任何真实服务器操作。
- 真实服务器、Xshell、SimpleSFTP、GPU 与 Agent 联调继续延期。
