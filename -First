 M .runtime/gateway-data/mcp-tool-settings.json
 M docs/target-mode-plan.md
 M test/ui/targetModePlan.test.js
# 目标模式当前计划：UI 操作逻辑与布局稳定性

本文档是当前目标执行源，只保留最新目标计划。上一轮已完成目标以 git 提交记录留存，不在本文档继续堆叠，避免后续执行代理把旧目标误当成当前工作。

`README.md` 仍是架构边界最高优先级。本轮不触发真实服务器部署，不新增远端直连路径。

## 全局约束

- 用户可见文案以中文为主，协议名、命令名、文件名保留原文。
- 当前架构保持 `Xshell 本地隧道 + Hub/Worker Agent + ZLK SFTP Manager`。
- 不新增直接 SSH、SCP、RSYNC 或绕过 Xshell 隧道的远端路径。
- SFTP 只用于低频真实文件传输，不用于实时状态、心跳、日志流或操作进度。
- 左侧资源树负责对象导航，中间列负责状态/结果/摘要，右侧工作详情负责操作/详情。
- UI 修改必须一步到位，避免临时版。
- 每批开始先刷新本文件的目标状态和验证清单；发现新故障先记录，再修复。
- 每批只实现一个独立闭环，跑对应轻量验证，通过后提交。
- 提交信息只描述已验证功能，不写未验证的大范围承诺。
- 不提交 `.runtime/gateway-data/mcp-tool-settings.json` 等无关本地状态。
- 最终全部批次完成后再运行全量验证、打包、全局安装，不重启 VS Code。



